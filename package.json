{
  "name": "zini-vai-mini",
  "private": true,
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "build": "npm --prefix frontend run build && ./backend/mvnw -f backend/pom.xml package",
    "test": "npm --prefix frontend test -- --watch=false && ./backend/mvnw -f backend/pom.xml test"
  }
}

